# work
### 一、功能
对灰度图像添加高斯+椒盐混合噪声，使用BM3D算法完成去噪处理，保存含噪图像与去噪图像，并计算PSNR（峰值信噪比）、SSIM（结构相似性）评价指标。

### 二、参数说明
| 参数               | 说明                     |
|--------------------|--------------------------|
| DATASET_PATH       | 原始灰度图像路径         |
| NOISY_SAVE_PATH    | 含噪图像保存路径         |
| RESULT_SAVE_PATH   | 去噪结果保存路径         |
| GAUSSIAN_SIGMA     | 高斯噪声标准差（σ=25）|
| SALT_PEPPER_AMOUNT | 椒盐噪声比例（2%）|

### 三、流程
1. 读取灰度图像并归一化至0-1范围；
2. 调用`add_mixed_noise`函数添加混合噪声，保存含噪图像；
3. 调用BM3D算法去噪，保存去噪图像；
4. 计算并输出PSNR、SSIM评价指标；

### 四、注意事项
1. 仅支持灰度图像处理，所有图像均做0-1归一化；
2. 输出图像命名规则：`原文件名_mixed_noisy.png`（含噪）、`原文件名_mixed_denoised.png`（去噪）；
